# D2 Verification Ledger

Tracks every change marked `# 4VERIFY (owner)` (D2) until the owner has
verified it. A change is **not "done"** until its row here is ✅ (see
[DEVELOPMENT_REQUIREMENTS.md](DEVELOPMENT_REQUIREMENTS.md) **D2**).

**Types** (how the item is verified):
- **A — provably equivalent.** A characterization/equivalence test pins the new behavior to
  the prior one. Verify = *review the test, then approve*. The agent may add these autonomously
  (the test is the proof, not the agent's opinion).
- **B — behavior change.** Pinned by a test, but the owner must *decide the change is desired*.
- **C — new math.** Needs the owner's domain review of the formula (a numeric brief helps).

**Status:** ⏳ ready for owner review · ❗ needs owner decision · ✅ owner-verified.

## Type A — provably equivalent (verify = review the test)

| Item | What changed | Pinning test | Status |
|---|---|---|---|
| T23.1 resample model | `DEFAULT_RESAMPLE_MODEL` == pre-migration enum map, minus only `mark_price`/`mark_iv` (unused, dropped) | `options/dictionary/migration_equivalence_test::test_resample_model_matches_pre_t23_1_enum`, `…::test_dropped_resample_columns_are_gone` | ⏳ |
| T23.1 column membership | `FUTURES_COLUMN_NAMES`/`SPOT_COLUMN_NAMES` == pre-migration `FuturesColumns`/`SpotColumns` (1:1 by value) | `…migration_equivalence_test::test_{futures,spot}_column_membership_matches_pre_t23_1_enum` | ✅ |
| T12 cache | `cachetools.TTLCache` rewrite is transparent: memoize + isolated deep copy; `None` never cached | `io/exchange/cache_test` | ⏳ |
| T14 `drop_na_price` | default reproduces the prior silent `dropna(subset=[PRICE])`; flag opts out | `options/option_data_drop_na_test` | ⏳ |
| Term registry rename | `Col`→`Term`, `OptionsCol`→`OptionsTerm`, `_columns.py`→`_terms.py` (627 call sites); reframed R4.3 as a **term** registry (one name per concept, used as column / variable / parameter — not "columns"). String values **identical**; pure identifier+doc rename | full suite green (254) + registry-value assertion (`Term.PRICE=="price"`, …) | ⏳ |
| Term domain split (R4.3) | derivatives terms (IV/greeks/`exch_mark_*`/`expiration_date`/underlying link, +`contract_kind`) moved from core `Term` to `OptionsTerm(Term)`; core `Term` left **neutral only** (identity/kind/price/time, 26 terms). String values **identical**; relocation only — `OptionsTerm` still exposes every term via inheritance (`column_names` includes inherited) | full suite green (263); partition check (`IV` on `OptionsTerm` not `Term`, `PRICE` on both) | ⏳ |
| Legacy-spec domain split (R4.3) | `core/migration` rebuilt around `MigrationSpec(renames, value_maps, timestamp_cols)` + `.merged()`; `CORE_SPEC` = neutral renames only, `options/migration.OPTIONS_SPEC = CORE_SPEC.merged(OPTIONS_MIGRATION_SPEC)` (derivatives renames/value codes). Same legacy→current mapping, re-partitioned by domain; `rename_legacy_option_columns` = options read-shim | `core/migration/legacy_parquet_test` + `read_shim_test` (neutral), `options/migration/legacy_columns_test` (derivatives renames + value codes) | ⏳ |
| ContractKind → options (R4.5) | `ContractKind` StrEnum (vanilla/combo/cso/stir) moved from `core/dictionary/_classification` to `options/dictionary/_classification` (a spot row has no contract kind; a future spot product would define its own). Importers (deribit/moex/`_abstract_exchange`/etl) now take `InstrumentKind` from core + `ContractKind` from options. Members/values **identical**; relocation only | `io/exchange/instrument_kind_mapping_test` (resolve_instrument_kind venue map unchanged), full suite green (263) | ⏳ |
| T20 Deribit normalize vectorization | row-wise `df.apply(_kind_enrichment, axis="columns")` (deep copy per ~5000 rows) → vectorized `_enrich_kinds` (split `exch_symbol` once, classify by token count with masks, per-mask expiration/strike/right/underlying + same-currency price conversion). 197× faster (313 ms vs 61.7 s on a 5040-row snapshot). Date cols → `datetime64[ns, UTC]`, strike → `float64` (the old expand-inferred dtypes). Behavior unchanged | `io/exchange/deribit_normalize_characterization_test` (new path == the verbatim pre-T20 row-wise oracle on combined/future/future_combo/option/spot books) | ⏳ |

## Type B — behavior change (owner must approve)

| Item | Change & rationale | Pinning test | Status |
|---|---|---|---|
| R9/R10 product-server architecture | Architecture contract update: R9 defines the product API tiers (core/domain, research facade, service/use-case, adapters) and R10 defines framework-neutral server contracts for dependency injection, serialization, validation, long-running jobs, and the server endpoint contract (`asset_code` + strategy/params in; fixed deterministic use-case; FastAPI as adapter; composition root injects provider/policy; response includes resolved inputs/context). No runtime behavior change in this row; rollout tasks A1-A4 remain separately D2-gated when they change public contracts or data semantics. | Docs review: `docs/dev/ARCHITECTURE_REQUIREMENTS.md`, `docs/dev/api-tiers.md`, `_forge/design/product-server-architecture.md`, `_forge/TASKS.md` | ✅ |
| A1 public export contract | Public-surface widening (the A1-gated change R9/R10 anticipated): `alphavar.options.__all__` now also exports the facade components `OptionsForecast` and `OptionsValidation` (already wired into `Option` as `.forecast`/`.validation`, previously importable only by full module path). Aligns the declared tier-2 surface in `docs/dev/api-tiers.md` with the exports. Additive only — no symbol removed/renamed, no runtime/data-shape change. | Import smoke: `from alphavar.options import OptionsForecast, OptionsValidation`; `docs/dev/api-tiers.md` tier-2 inventory | ✅ |
| ADR 0004 lib/class contract boundaries | Architecture contract update: R1.1/R1.2 lock explicit `lib` shapes, no hidden upstream resolution, storage adapters outside `lib`; R3 pins facades as binding layer; R4.4 pins result/interchange schemas as chain contracts; R8 tightens Polars-friendly no-in-place direction. Creates remediation tasks T41-T44. No runtime behavior change in this row; implementation tasks remain separately D2-gated when they touch code/data. | Docs review: `docs/dev/ARCHITECTURE_REQUIREMENTS.md`, `docs/dev/decisions/0004-lib-class-contract-boundaries.md`, `_forge/design/architecture-remediation.md`, `_forge/TASKS.md` | ⏳ |
| ADR 0006 dataset storage and resolution | Architecture contract update: local data is dataset→exchange→asset; metadata is YAML beside datasets/assets/listings; reference is domain-specific tables under `reference/`; `{exchange}:{asset}` is resolver/input shorthand; `economic_asset_id` is resolver-only catalog knowledge for cross-listing discovery. Creates implementation tasks T45-T48 and supersedes the unverified `_asset.json`/`_meta.parquet` storage layout. | Docs review: `docs/dev/decisions/0006-dataset-storage-and-resolution.md`, `docs/dev/ARCHITECTURE_REQUIREMENTS.md`, `README.md`, `_forge/TASKS.md` | ⏳ |
| T42a analytic tidy producer contracts | Pinned interchange schemas for five single-frame options producers on the `core.disc` surface: `chain` (`ChainSchema`), `desk` (`DeskSchema`), `time_value_series` (`TimeValueSeriesSchema`), and payoff split into `payoff_legs` (`PayoffLegsSchema`) + `payoff_curve` (`PayoffCurveSchema`) — new pandera result schemas in `options/schemas/_result_schemas.py` + `DataFrame[...]` return annotations (inert at runtime) + `register(...)` with explicit input edges (`chain`←`options_history`, `desk`←`chain`, `time_value_series`←`options_history`, `payoff_legs`←`chain`, `payoff_curve`←`payoff_legs`). **Payoff refactor** (owner-ruled variant a): `chain_payoff` now delegates to `payoff_legs` (per-leg P&L) + `payoff_curve` (per-strike sum over legs) and still returns the `(curve, legs)` tuple — same outputs, split into two producers with a real edge. **Risk-column vocabulary** promoted from the local `RiskColumns` enum to neutral `ResultTerm` (`RISK_PNL`/`RISK_PNL_PREMIUM`/`LEG_ID`) — string values identical, `RiskColumns` removed; behavior-preserving rename. No math/data-shape change; the `_calc_premium_profile` mark-to-market math stays separately owner-gated (T14b ❗). | `options/producers_test` (registration + each producer's real output validates against its schema + `payoff_curve` == per-strike sum of `payoff_legs`), `options/lib/analytic/risk/risk_payoff_test` (migrated to `ResultTerm`), `flow` suite green | ⏳ |
| T41 pure-`lib` boundary (storage move) | Relocated the reference storage adapter out of pure `lib`: `read_reference`/`write_reference`/`asset_meta_path`/`contract_history_path` moved from `options/lib/reference/_store.py` → `io/provider/_reference_store.py` (exported via `alphavar.io.provider`); consumers re-pointed (file provider, ETL `_fold_reference`/`reference_migration`, `data_migration` tool). **Pure move** — same on-disk layout (`_asset.json` + `_meta.parquet` at the asset root), same signatures, no behavior/data-shape change. An AST guard now fails if `options/lib` imports `alphavar.io`/a facade or calls file/network I/O (`open`/`os.makedirs`/`read_parquet`/`to_parquet`/…). Restores R1. | `options/lib/purity_guard_test` (per-module purity), `io/provider/reference_store_test` (relocated round-trip), reference + `etl_reference_fold` suites green | ⏳ |
| T11a Deribit `kind` wire token | `DeribitAssetKind.value` is the singular venue token (`option`/`future`), decoupled from `InstrumentKind` (`options`) — fixes a silent HTTP 400 (R2.2). **MOEX audit: clean** — MOEX sends `MoexAssetType.value` (venue-native) on the wire; project enums are used only to normalize responses. | `io/exchange/instrument_kind_mapping_test::test_deribit_asset_kind_values_are_venue_native` | ⏳ |
| T23.1 resample sort columns | latent bug fixed: the prior list mixed a `.nm` string with two bare enum members, so `exch_timestamp`/`request_timestamp` never matched and were dropped from the sort; now all three are real column names (deterministic multi-key sort) | `…migration_equivalence_test::test_resample_sort_columns_include_all_three_timestamps` | ⏳ |
| T23.6 raw column convention | the pre-currency-conversion raw value is now stored as `<col>_raw` (was the `source_<col>` prefix); on load, `rename_legacy_columns` maps legacy names (incl. `source_*`→`*_raw`) to the current dictionary, idempotently | `core/migration/read_shim_test` | ⏳ |
| T23.9b Deribit asset_code/exch_symbol split | the Deribit normalizer now emits `asset_code`=underlying + `exch_symbol`=venue contract (was `asset_code`=contract / `base_code`=underlying), matching the already-split stored schema; `deribit_etl` routes by `asset_code` | `io/exchange/deribit_market_test`, `options/etl/deribit_etl_test` | ⏳ |
| T23.9b MOEX asset_code/exch_symbol split + BASE_CODE elimination | MOEX normalizer/join/ETL aligned to the canon (`asset_code`=underlying, `exch_symbol`=contract); the underlying-future merge re-keyed onto the contract; `base_code` eliminated from the live pipeline (both venues), `timeframe_resample` multi-asset grouping → `asset_code` | `io/exchange/moex_test` (book-summary now hermetic), `moex_options_test`, `options/etl/moex_etl_test`, `…/timeframe_resample_test` | ⏳ |
| T23.6 exch_price / interim price (R4.2) | `fill_option_price` → `exch_price` (venue traded); Deribit currency-converts it (+`exch_price_raw`); our `price` mirrored from `exch_price` via `source_interim_price` (interim until smile-fit). Behavior-preserving (same value still in `price`); `exch_iv` left nullable | `options/lib/normalization/price_normalization_test`, `io/exchange/deribit_market_test` (price/exch_price populated) | ⏳ |
| T23.5 schema nullability + boundary validation | entity schemas relaxed to match reality (`price`, `iv`, futures `expiration_date` → nullable; greeks already optional); `validate_book_data` checks each kind (`OptionsHistory`/`FuturesHistory`/`SpotHistory`) at the exchange→storage seam in both ETLs, gated by `ALPHAVAR_VALIDATE` | `options/etl/validate_book_data_test`; committed fixtures + live deribit/moex frames validate | ⏳ |
| T25 reference split (inc.1) | `split_reference`/`apply_reference` factor per-instrument constants (asset-level → `AssetMeta`; contract-level → deduped frame keyed by `(expiration,strike,option_right)`, **only columns constant per key**) out of a quote frame, losslessly; −26% memory on the BTC fixture | `options/lib/reference/split_test` (lossless round-trip + extraction + reject cases) | ✅ |
| T25 reference SCD (inc.2) | SCD Type 2: `as_of` (version valid at a date, `valid_from` <= t < `valid_to`) + `append_on_change` (new key → open; changed attr → close prior + open new; unchanged → no-op; missing key left open) | `options/lib/reference/scd_test` (6 cases incl. exclusive-`valid_to` boundary) | ✅ |
| T25 reference storage (inc.3) | Superseded by ADR 0006 before owner verification: `_asset.json` + `_meta.parquet` is not the target storage contract. Target layout is dataset→exchange→asset with YAML metadata and domain reference tables (`reference/option_contracts.parquet`, `future_contracts.parquet`, `contract_specs.parquet`). Implementation continues under T45-T47. | ADR review: `docs/dev/decisions/0006-dataset-storage-and-resolution.md`; implementation tasks T45-T47 | ❗ superseded; do not close as-is |
| T25 reference read-wiring (inc.4A) | additive read-side: `Provider.load_reference` (default `(None, empty)`; file provider reads the asset-root reference), `OptionsData.reference`/`.reference_history`/`reapply_reference` (idempotent asset-level broadcast, no-op when absent), `Option.reference`. Existing `df_hist`/`df_fut` load path unchanged — behavior preserved (reference `None` pre-migration) | `io/provider/file_provider_test` (absent→None, round-trip), `options/option_data_class_test` (no-op, broadcast, no-overwrite) | ⏳ |
| T25 reference migration (inc.5) | extract-only wide→sidecars: `extract_reference(df, when)` (split + seed one open contract version at the earliest observed `when`) + `options/etl/reference_migration` driver (dry-run/`--apply`/CLI, per-asset, writes `_asset.json`+`_meta.parquet`). **Wide series files left untouched** (additive; slimming deferred to the as-of rejoin). Dry-run on the BTC fixture: 838 contracts | `options/lib/reference/migration_test` (asset-meta + one-open-per-contract, empty, sidecars-written + series-untouched, skip-no-options) | ⏳ |
| T25 reference ETL fold (inc.4B) | `EtlHistory._fold_reference` folds the options reference into the SCD-2 sidecar after each history write (gated `update_reference=True`, `when`=batch's latest obs): `split_reference`→`append_on_change` (new→open / changed→close+open / unchanged→no-op). Options-only, additive (sidecar only, never the series), guarded (failure logged, never aborts the write) | `options/etl/etl_reference_fold_test` (writes sidecar, append-on-change, unchanged no-op, skip-non-options) | ⏳ |
| T25 series slimming | load: `join_reference_asof` (per-row interval as-of join) + `OptionsData._restore_reference` reattach reference, **absent columns only** → slim & legacy-wide read identically. write: `EtlHistory._to_stored_series` writes `split_reference().quotes` — opt-in `slim_series=False`, gated on `update_reference` (reversible). Keystone: wide→slim+sidecar→load round-trip is lossless through the real `OptionsData` path | `options/lib/reference/slim_roundtrip_test` (lossless round-trip), `scd_test` (as-of per-row time / unmatched NaN / no-overwrite), `etl_reference_fold_test` (slim gated) | ⏳ |
| T19 period-load date semantics | `_load_data_for_period` implemented (was `NotImplementedError` fences). Owner-agreed semantics: a request is always an **inclusive range**; an open bound resolves to the stored data edge — `period_to` alone = everything up to & including it, `period_from` alone = from it through the latest data, neither = all stored data. `date`=whole day, `datetime`=exact instant (`timestamp <= to` / `>= from`, naive bound aligned to the column tz), `int`=year edge; a year file absent inside the resolved span is skipped + warned. `isinstance` dispatch (datetime before date); magic `"datetime"` literal → `OptionsTerm.TIMESTAMP`. `load_options_chain` returns `None` (local files store no pre-selected chain → facade builds from history) | `io/provider/local_provider_test` (8 period cases incl. inclusive ceiling/floor/range/missing-year-warn), `options/{chain_class,option_class}_test` (chain-from-history, ex-xfail) | ⏳ |
| T24 resample gap fill (LOCF/NOCB) | latent no-op fixed: `timeframe_resample` pre-resample fill was `df[cols].infer_objects(copy=False).ffill(inplace=True)` on a **throwaway column slice** → did nothing. Now `df[cols] = df[cols].infer_objects(copy=False).ffill()` / `.bfill()`, per leaf contract → an all-NaN target bucket inherits the last known `last` value (LOCF) / next known `first` value (NOCB) instead of staying NaN. **Owner-approved behavior change** (chosen over delete-the-dead-lines); confined to multi-bucket/intraday targets with gaps — the EOD path (1 bucket/day) is unaffected | `options/lib/normalization/timeframe_resample_test::test_convert_to_timeframe_fills_gap_buckets` (gap bucket → carried values, not NaN); existing EOD resample tests unchanged | ⏳ |
| T21 validation component | new `options/lib/validation` semantic data-quality checks (owner-scoped, **non-mutating** report). **Input gate** (`validate_input`): completeness (no null/zero price/iv), no-arb price bounds (call ≤ F / put ≤ K upper breach = scale/parse ERROR, below intrinsic = WARNING), strike sanity band, timestamp tz-aware + timeframe-grid alignment (+ resolution-independent sub-ms warning), duplicates on the natural key. **Model checks** (`validate_model`): positive IV, smile butterfly (Gatheral g(k)) + calendar (ATM total variance monotone in expiry) no-arb, fit residual. **`clean`** = opt-in remediation (defaults off). Facade `OptionsValidation` → `Option.validation`. Owner must approve the rules/tolerances/severities | `tests/unit/options/lib/validation_test` (21: each check, report raise, clean no-op/fixes, facade input+clean+model) | ⏳ |

## Type C — new math (owner domain review)

| Item | What | Evidence | Status |
|---|---|---|---|
| T14b payoff `_calc_premium_profile` | new mark-to-market per-strike P&L for `RISK_PNL_PREMIUM` (was `NotImplementedError`); aggregation sums it alongside `RISK_PNL` | `options/lib/analytic/risk/risk_payoff_test` (30 pass); prior version preserved commented in `payoff.py` | ❗ needs numeric brief + owner review |
| T21 Black-76 pricer | new `options/lib/pricer` (Black-76 forward price, vega, bisection implied-vol) + df helpers (`add_model_iv`/`add_fair_price`, ACT/365 tenor, `F`=underlying_price) + `OptionsPricer` facade | `options/lib/pricer/black_scholes_test` (ATM ref, parity, round-trip IV, intrinsic, NaN bracket), `…/enrich_test` | ❗ owner review of the model/conventions (forward model, rate=0, ACT/365) |
| T21 smile-fit / no-arb | `options/lib/pricer/smile` — SVI (raw, Gatheral; quasi-explicit calibration), quadratic-in-log-moneyness, SABR (β=1 Hagan) behind `make_smile_model` (default SVI); pure numpy + hand-rolled Nelder–Mead (no scipy). Butterfly no-arb via Gatheral `g(k)`. DF driver `add_smile_iv` fits one smile per `(asset,expiration,timestamp)` slice in `k=ln(K/F)` and samples the model `iv`; `OptionsPricer.fit_smile` writes `iv` + Black-76 `price` → smooth arbitrage-checked model output (replaces the T23.6 `exch_*` mirror) | `options/lib/pricer/smile_test` (SVI recovers a known slice <1e-3, all-model fit, no-arb detect + steep-skew flag, degenerate/NaN), `…/smile_enrich_test` (per-slice fit, no-mutation, missing-IV raise, facade end-to-end) | ❗ owner review of the parametrizations (SVI quasi-explicit calibration, SABR β=1, the g(k) no-arb criterion, k=ln(K/F) convention) |
| T27 forecast price models (it.1) | new `options/lib/forecast` model factory (Target × Process × Engine). Price target: `random_walk` (driftless log RW), `gbm` (log-drift ν=mean(r)/dt, σ²=var(r)/dt → lognormal terminal `ln S_T ~ N(lnS₀+ν·H, σ²·H)`), `garch` GARCH(1,1) Gaussian-MLE (pure-numpy Nelder–Mead, unconstrained reparam ω=exp / persistence φ & α-share via logistic ⇒ ω>0, α,β≥0, α+β<1; MC simulates round(H/dt) steps). Engines `analytic` (closed-form lognormal) / `montecarlo`; `norm_ppf` = Acklam inverse-normal CDF; horizon ACT/365. Facade `OptionsForecast` → `Option.forecast` | `tests/unit/options/lib/forecast/` (21: norm_ppf↔norm_cdf round-trip, horizon norm, gbm analytic↔MC convergence, RW median=spot, GARCH positivity/stationarity + analytic-raises, factory planned/unknown, series + facade) | ❗ owner review of the estimators (GBM ν & σ² annualization, GARCH(1,1) MLE + reparam + n_steps=round(H/dt), lognormal ppf/mean, Acklam norm_ppf, ACT/365 horizon, default series = most-populated expiration) |

| T27 forecast price models (it.5) | endogenous/model-free price processes in `options/lib/forecast/price`: `ar1` (OLS AR(1) on log-price `x_t=c+φ·x_{t-1}+ε`, σ_ε² with dof m−2, φ clipped to ±0.9999; n=round(H/dt)-step **log-normal** terminal `meanlog=μ+φ^n(x_T−μ)`, `var=σ_ε²(1−φ^{2n})/(1−φ²)`, μ=c/(1−φ) — analytic + MC via `LogNormalPrice`), `empirical` (model-free: horizon return = Σ of n resampled historical log-returns, `S_T=S₀·exp(Σr)`; no analytic form). New `bootstrap` **engine** via a `FittedProcess.bootstrap_terminal` hook — `empirical` does i.i.d. resample (`montecarlo`) vs **moving-block** wrap-around resample (`bootstrap`, block=round(n^{1/3})). New `front` price **source** (`_series.front_price_series`): per-ts nearest expiry ≥ now+roll_buffer, **proportional back-adjustment** ρ_r=P_B(t_r)/P_A(t_r) anchored at the latest leg. Factor-conditional `factor_linear`/`var` still deferred (ADR 0003 input contract) | `tests/unit/options/lib/forecast/forecast_models_test.py` (ar1 analytic↔MC, ar1 mean-reversion of the median; empirical positivity/centered + no-analytic; bootstrap reproducible + rejects non-empirical models), `forecast_series_test.py` (front roll length/last-leg/return-continuity; facade front + it.5 models), `forecast_factory_test.py` (ar1/empirical available, bootstrap engine, factor_linear/var still planned) | ❗ owner review of the math (AR(1) OLS estimator + n-step log-normal mean/variance recursion + φ clip; empirical Σ-of-resampled-returns terminal; i.i.d. vs moving-block bootstrap + n^{1/3} block; front roll selection + proportional back-adjustment) |

| T27 forecast vol models (it.2) | vol target in `options/lib/forecast/vol`: `realized` (√(var(r)/dt)), `ewma` (RiskMetrics λ=0.94 EWMA variance, flat term structure), `garch` (shared `_garch.estimate_garch`; analytic variance term structure `E[σ²_{t+k}]=σ²_unc+(α+β)^{k-1}(σ²_{t+1}−σ²_unc)` → horizon-avg expected vol, + MC realized-vol = √(mean ε²_k/dt)), `har` (Corsi HAR-RV, RV=r² proxy, d/w/m=1/5/22, OLS via lstsq, iterated multi-step, realized fallback if short). Observable = annualized vol over horizon; `DegenerateTerminal` wraps point forecasts. GARCH(1,1) MLE refactored into shared `_garch.py` (price + vol; price garch behavior unchanged) | `tests/unit/options/lib/forecast/forecast_vol_test.py` (8: realized=√(var/dt), EWMA recursion, EWMA point, GARCH analytic≈MC + both engines, HAR run + short-fallback, facade vol) | ❗ owner review of the estimators (EWMA λ & flat term structure, GARCH variance term structure + expected-vol = √(mean var), HAR RV=r² proxy + d/w/m windows + multi-step iteration, ACT/365 annualization) |

| T27 forecast surface models (it.4) | surface target in `options/lib/forecast/surface`: state = SVI θ stacked across **constant-maturity tenor nodes** (default 1w/2w/1m/2m/3m). Per timestamp: fit a smile per expiration, interpolate **total variance** across expirations to each node (`interp_total_variance`: linear-in-w between bracketing tenors, flat `w/τ` extrapolation beyond range = T-extrapolation), refit SVI per node → stacked θ. Models reuse the verified smile θ-dynamics on the longer vector: `svi_surface` (RW, default) / `svi_surface_var` (VAR(1)) / `pca_factor` (PCA). Engines `analytic` / `montecarlo` → `SurfaceForecast` (expected surface + scenario σ(k,τ) bands; `decode_surface` node-major reshape; iv(k,τ) by the same tenor interp). No-arb: butterfly per node + **calendar** (ATM w non-decreasing across nodes). The same interpolation backs the **`constant_maturity` smile convention (B)** — single-node CM history at the target tenor (vs `fixed_expiration` A). Facade `OptionsForecast.surface` / `.smile(maturity='constant_maturity')` | `tests/unit/options/lib/forecast/forecast_surface_test.py` (10: interp linear+extrap+single, decode round-trip + wrong-size, factory map/planned, stacked-θ→SurfaceForecast bands+to_frame, CM history shape, facade surface end-to-end + default-nodes/analytic + smile CM convention) | ❗ owner review of the surface math (constant-maturity total-variance interpolation + T-extrapolation, per-node SVI refit, stacked-θ dynamics reuse, calendar no-arb check, CM convention B) |

| T27 forecast smile models (it.3) | smile target in `options/lib/forecast/smile`: forecast the calibrated SVI θ=(a,b,ρ,m,σ) of one expiration over history, decode the terminal θ back to a smile at target tenor τ=E−(as_of+H) reusing raw-SVI `_raw_w` (domain clamp b≥0,\|ρ\|<1,σ>0 + w-floor). Every model is a Gaussian terminal on θ (mean+cov): `param_rw` (driftless RW: mean=last θ, cov=n_steps·cov(Δθ)), `param_var` (VAR(1) θ_t=c+θ_{t-1}·A+ε via OLS, mean iterated forward, cov recursion Vₖ=AᵀVₖ₋₁A+Σ_ε; driftless-RW fallback when under-identified), `param_pca` (RW on top-k PCA modes of cov(Δθ)). Engines `analytic` (expected smile) / `montecarlo` (PSD-safe MVN θ draws → σ(k) quantile bands via `decode_smile`/`sample_mvn`). Maturity convention **A `fixed_expiration`** built (mixes tenors across history — flagged), **B `constant_maturity`** raises (planned w/ surface it.4). `SmileForecast` result (expected_smile / iv_quantiles / scenario_smiles / to_frame / is_butterfly_free). Facade `OptionsForecast.smile` (own sibling factory `make_smile_forecast_model`, scalar `make_forecast_model(SMILE,…)` redirects). θ history = per-timestamp SVI fit in k=ln(K/F) (`build_theta_history`) | `tests/unit/options/lib/forecast/forecast_smile_test.py` (13: decode round-trip + clamp, RW driftless+cov-scaling, VAR revert+short-fallback, PCA rank-limit, analytic degenerate vs MC widening bands, to_frame cols, factory unknown/planned/redirect, θ-history recovers known slice, facade end-to-end + expired-horizon + all-models) | ❗ owner review of the θ dynamics (fixed_expiration tenor-mixing, RW/VAR/PCA terminal mean+cov, decode-at-target-tenor σ=√(w/τ), MVN eigh-clip sampler, default = most-populated expiration) |

## How to clear an item
1. Owner reviews the pinning test (Type A/B) or the math + numeric brief (Type C).
2. On approval: set the row to ✅ here, drop the `# 4VERIFY` marker in the code, and mark
   the task **Done** (not "pending owner verification") in `_forge/TASKS.md`.
