name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Akmon generated pointers
        run: python3 _aitna/akmon/bin/sync.py --check

      - name: Akmon verify
        run: python3 _aitna/akmon/bin/verify.py --strict

      - name: Akmon self fixture verify
        run: python3 _aitna/akmon/meta/self_ci.py

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.14"

      - name: Sync dependencies (with etl extra + dev/test groups)
        run: uv sync --extra etl

      - name: Lint (ruff)
        run: uv run ruff check src tests tools

      - name: Tests (pytest, hermetic fixtures)
        run: uv run pytest

      - name: Akmon tests
        run: uv run pytest _aitna/akmon/meta/tests
