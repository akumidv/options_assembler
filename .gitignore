.git
/.idea/
/venv/
/.venv/
/.vscode/
__pycache__
.pytest_cache
/.tmp
.ipynb_checkpoints
scripts/_*
/data
*.tar
/dist/
/build/
*.egg-info/
TODO.txt
/demo/**/_*.*
node_modules/
.next/
docs/phd/
.gitmodules
*.svg

# secrets
*.env
!*.env.example

# _akmon model routing — per-user/per-session artifacts
.claude/model-routing.local.json
.claude/model-routing.log
.claude/second-opinion/
# generated k-* subagent defs: model: frontmatter is per-user (follows the local
# orchestrator), regenerated by the model-routing hook — tracking them would churn
.claude/agents/k-*.md
# transient git worktrees (EnterWorktree / manual) — never tracked
.claude/worktrees/

# local agent artifacts (reports, findings snapshots) — never committed
/_aitna/artifacts/
